---
interface Props { seconds: number; }
const { seconds } = Astro.props;
---
<div class="comic-card flex items-center gap-3 bg-lava px-5 py-3" role="timer" aria-label={`${seconds} second quiz timer`}><span class="text-3xl animate-pulse">⏱️</span><strong class="text-2xl">{seconds}s</strong></div>
